cc.exports.wwGameConst = {
	SERVER_IP = "server_ip"; --store connect ip key into local storeage
	SERVER_PORT = "server_port"; --store connect port key into local storeage
	FIRST_TOUCH = "first_touch", -- store first touch finger key
	IS_NET_ERROR = "IS_NET_ERROR", --is network error
}

cc.exports.LOADING_MODE = {
	MODE_NORMAL = 0,
	MODE_TOUCH_CLOSE = 1,
}